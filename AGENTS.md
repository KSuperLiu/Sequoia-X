# Sequoia-X 维护说明

## 默认沟通

- 默认使用中文回复和记录，除非用户明确要求其他语言。

## 市值过滤与数据采集逻辑

- 日常跑数入口仍然是 `python main.py`。
- 日常跑数不再联网实时查询市值，而是只读取 SQLite 本地表 `stock_market_cap`。
- 本地市值表字段包括 `symbol`、`name`、`market_cap`、`updated_at`。
- 行情采集前会从 `stock_market_cap` 读取 `market_cap >= 5_000_000_000` 的股票代码，即总市值不低于 50 亿元的股票。
- 总市值小于 50 亿元的股票不会进入 baostock 行情采集任务。
- `python main.py --backfill` 也使用同一套本地市值表过滤，只回填总市值不低于 50 亿元的股票历史 K 线。
- 如果 `stock_market_cap` 为空，或没有任何股票满足 50 亿元条件，日常采集会直接跳过，避免误采小市值股票。

## 手动刷新本地市值表

- 用户手动要求更新本地市值表时，运行：

```powershell
.\.venv\Scripts\python.exe main.py --refresh-market-cap
```

- 刷新流程先通过 baostock 获取当前上市 A 股列表。
- 然后批量调用腾讯行情接口 `https://qt.gtimg.cn/q=...` 获取实时/延迟行情。
- 腾讯返回值是 `~` 分隔字段，其中第 45 个字段按当前实现视为“总市值”，单位是“亿元”。
- 代码中使用 Python 下标 `44` 读取该字段，并乘以 `100_000_000` 转换为元后写入 `stock_market_cap.market_cap`。
- 最近一次验证中，腾讯接口成功写入 5203 条本地市值记录，其中 2894 只股票满足总市值不低于 50 亿元。

## 运行耗时基准

- 最近一次完整测试命令：`python main.py`。
- 测试结果：本地表筛出 50 亿元以上股票 2894 只，其中 1535 只需要更新行情。
- 完整流程耗时约 6 分 03 秒。
- 行情采集阶段约 4 分 44 秒，写入 1533 条数据，失败 0。
- 策略与飞书推送均正常完成。

## 注意事项

- 不要在日常跑数中恢复每次在线查询市值，否则会重新引入东方财富/AkShare 接口不稳定的问题。
- 如需更新市值范围，先刷新 `stock_market_cap`，再执行日常跑数。
- 如果腾讯字段定义变化，需要重新核对第 45 个字段是否仍为总市值。
